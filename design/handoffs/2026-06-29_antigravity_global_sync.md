# Antigravity Global Sync Readback

Date: 2026-06-29
Risk: R1, global agent-instruction text only
Scope: Antigravity/Gemini global context target. No MCP, no hooks, no repo fan-out.

## Evidence

Official docs checked:

- `https://antigravity.google/docs/rules-and-workflows`
- `https://antigravity.google/docs/mcp-server`
- `https://antigravity.google/docs/gcli-migration`

Relevant local evidence:

- `C:\Users\dszub\.gemini\config\mcp_config.json` exists.
- `C:\Users\dszub\.gemini\antigravity-ide\mcp_config.json` exists.
- `C:\Users\dszub\.gemini\GEMINI.md` did not exist before this slice.
- Antigravity state exists under `C:\Users\dszub\.gemini\antigravity`.

## What Changed

Added a managed Antigravity/Gemini global context target:

```text
C:/Users/dszub/.gemini/GEMINI.md
```

Tracked source:

```text
agent-rules/overlays/antigravity-global.md
```

The target is generated by:

```powershell
python scripts/sync_agent_rules.py --write --target antigravity-global --init-managed-blocks
```

## Runtime Readback

Latest readback:

```text
python scripts/sync_agent_rules.py --check --target antigravity-global
antigravity-global: clean

python scripts/session_cost_probe.py check --baseline design/baselines/workflow_os_a1_baseline.json
antigravity_global.kernel_ok=true
antigravity_global.bytes_delta=0
antigravity_global.lines_delta=0
antigravity_global.sha_changed=false
sync_check.ok=true
```

The A1 baseline now covers Claude, Codex, Cline, and Antigravity global text-rule surfaces.

## Validation

```text
python -m ruff check scripts\sync_agent_rules.py scripts\session_cost_probe.py scripts\tests\test_sync_agent_rules.py scripts\tests\test_session_cost_probe.py
All checks passed

python -m pytest scripts\tests
55 passed
```

## Boundaries

- Did not run per-repo `sync --write --repo`.
- Did not create repo-local `GEMINI.md`, `AGENTS.md`, or `.agents`.
- Did not configure `.agents/hooks.json`, `.agents/mcp_config.json`, or any global MCP JSON.
- Did not edit TSU/Tsignal runtime, broker, order-path, or trading config.

## Remaining Gates

- Antigravity MCP/hooks require a separate explicit slice with schema-level validation.
- Headroom/RTK remains deferred until the real B0 mixed-session baseline exists.
- §3.7 mechanical write-deny remains R2/R3 and needs separate explicit operator GO.
