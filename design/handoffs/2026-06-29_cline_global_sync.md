# Cline Global Sync Readback

Date: 2026-06-29
Risk: R1, global agent-instruction text only
Scope: Cline global rules target. No repo fan-out, no hooks, no Antigravity config.

## What Changed

Added a managed Cline global sync target:

```text
C:/Users/dszub/.clinerules/agent-rules.md
```

Tracked source:

```text
agent-rules/overlays/cline-global.md
```

The target is generated by:

```powershell
python scripts/sync_agent_rules.py --write --target cline-global --init-managed-blocks
```

The sync machinery now includes a parent-directory lock fix, because creating a brand-new global target previously failed before the atomic writer could create the parent directory.

## Runtime Readback

Latest readback:

```text
python scripts/sync_agent_rules.py --check --target cline-global
cline-global: clean

python scripts/session_cost_probe.py check --baseline design/baselines/workflow_os_a1_baseline.json
cline_global.kernel_ok=true
cline_global.bytes_delta=0
cline_global.lines_delta=0
cline_global.sha_changed=false
sync_check.ok=true
```

The A1 baseline was refreshed after adding Cline, so future kernel drift checks cover Claude, Codex, and Cline as current managed global surfaces.

## Validation

```text
python -m ruff check scripts\sync_agent_rules.py scripts\session_cost_probe.py scripts\tests\test_sync_agent_rules.py scripts\tests\test_session_cost_probe.py
All checks passed

python -m pytest scripts\tests
54 passed
```

## Boundaries

- Did not run per-repo `sync --write --repo`.
- Did not create repo-local `.clinerules`.
- Did not configure `.clinerules/hooks`; Windows hooks remain out of scope.
- Did not edit Antigravity paths or MCP config.
- Did not touch TSU/Tsignal runtime, broker, order-path, or trading config.

## Remaining Gates

- Antigravity remains manual/deferred until exact paths/schema are proven from the running app or official local docs.
- Headroom/RTK remains deferred until the real B0 mixed-session baseline exists.
- §3.7 mechanical write-deny remains R2/R3 and needs separate explicit operator GO.
