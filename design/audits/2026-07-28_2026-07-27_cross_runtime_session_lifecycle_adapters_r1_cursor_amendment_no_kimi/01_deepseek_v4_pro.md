# Audit — DeepSeek V4 Pro

Model: `deepseek/deepseek-v4-pro`  
Tokens: 17,496  
Latency: 103.3s

---

We need to audit the implementation plan against the given criteria: architectural soundness, threading/async safety, edge cases, cost/performance, integration risks, and underspecified areas. Grade findings P1, P2, P3. End with TOP 3 CHANGES.

First, understand the plan: It's about cross-runtime session lifecycle adapters for Codex and Cursor, with Claude Code already done. It describes the Codex slice (already shipped) and Cursor discovery (complete, but implementation not started). The plan details architecture, implementation slices, failure maps, etc.

We need to audit the portions that are not yet implemented (Cursor CU0-L, CU1-CU4) and also check the overall design for the whole plan (including Codex part, but we can note issues with already shipped parts if they affect future work). However, the plan says Codex slice is shipped and active; we might find issues that could be relevant for Cursor adapter design. The audit should be about the plan as it stands. We can point out gaps.

Criteria:
1. Architectural soundness: solid design? missing components? unclear ownership?
2. Threading/async safety: race conditions, blocking I/O on hot paths, lock inversion? The adapters are CLI processes (one per event) and likely synchronous. Could there be concurrent events? The plan mentions adapter is a process per event. Potential race conditions in file writes? The existing engine uses fsync and atomic replace, but what about concurrent Codex and Claude sessions? The plan mentions session-id namespaced files, write-once bindings, so concurrency might be okay. However, might there be issues with the shared session_registry.json being written concurrently? It's only written by installer, not during session. The plan mentions installer handles replace atomically. So maybe fine.
3. Edge cases: unhandled failures, degraded mode gaps.
4. Cost/performance: excessive API calls? (no API calls, just local I/O). Memory leaks? Bottlenecks? For Codex part, measured p95 565ms start, 777ms end, which is slightly above aspirational targets but fine. For Cursor, no implementation yet, but we can consider potential performance issues: reading transcript from disk? Plan says no scanning. The adapter itself is lightweight. Might be fine.
5. Integration risks: gaps at system boundaries? contract mismatches? stale caches? Mainly between adapters and existing engine (router/lifecycle). Plan specifies input shapes. Does it align with existing engine contracts? The plan says existing router/lifecycle accept the payload directly. For Cursor, they'll need to map the native event to that shape. The plan doesn't detail the exact mapping, but CU1 will implement. Integration risk: if Cursor event shape differs from expected, adapter must handle. That's fine.
6. Underspecified areas: what needs more detail before implementation begins? For Cursor, CU0-L is required to capture actual payloads before writing adapter. That's a gate. So adapter can't be built without those fixtures. That's specified. But are there any assumptions that might break? For instance, "user-level activation preserves unknown hooks, validates the merged config, etc." The plan for Cursor activation is vague before CU3. Is the exact hook schema fully known? Discovery found documentation but no actual user-level activation. Might need to refine later. The plan is to capture payloads first, so adapter design will be based on real data. That's okay.

Now, what issues can we surface?

- Architectural: The plan mentions "The adapter owns only Cursor payload validation, event-name normalization, nullable transcript handling, and bounded host diagnostics." That's clear. However, there's a potential missing piece: the plan says "Cursor `sessionStart` context injection is accepted only when a live probe proves the model received the exact nonce." This implies a nonce mechanism. The nonce is part of the context injection? The existing engine likely injects context and includes a nonce to verify that the model saw it. But how does the adapter know the nonce? It would need to include a nonce in the output, and then the probe checks if the model response includes it. That's a live verification, not part of the adapter code. So the adapter just passes the context from the engine. The engine probably already generates a nonce per session? The plan says "nonce-proven context" for acceptance. The adapter doesn't generate nonce; the engine does. So that's okay. But if the engine doesn't generate a nonce for Cursor contexts, we might need to add that. However, the plan's DoD for Cursor says "nonce-proven context". The existing engine might have a nonce feature? It might be part of the session.plan.v1. The plan says "prove the model received the exact nonce." That's a live test, not part of adapter. So it's not missing in adapter, but might need a nonce in the context injection. The existing session_router might need to add a nonce to context for Cursor sessions? That's a gap: does the existing router/lifecycle engine support generating a nonce and injecting it into context for Cursor? It does for Claude? Probably not; the nonce concept might be new for verification. The plan says "Cursor sessionStart context injection is accepted only when a live probe proves the model received the exact nonce." That suggests that the engine must be updated to include a nonce. That's an architectural change that isn't described. The plan says "no new lifecycle policy or persistence schema", but adding a nonce to context might be a policy change? It's not a verdict policy; it's just a proof-of-delivery mechanism. The nonce can be a random string attached to the plan/context that the model will echo back. The live probe checks if the model response contains that nonce. That requires the adapter/engine to include it. The current engine for Claude may not have this. The plan does not specify how the nonce is generated and passed. That's underspecified for Cursor acceptance.

- Edge cases: Cursor sessionEnd when the conversation was not started from a sessionStart? Possibly if Cursor fires a sessionEnd without a prior start? The plan says "sessionEnd delegates only when the exact existing binding is valid." So if no binding, it's a no-op. That's okay.

- Integration risks: The plan says for Codex, the transcript projection handled Codex format. For Cursor, projection is conditional on stable format. If Cursor transcript format is stable and different, will it be projected by the same dual-format projection? The plan says "transcript projection, conditional" and will be added only if stable. That's fine. But there might be integration with curator_claims.py. That's future.

- Underspecified: The Cursor adapter's exact mapping from native event to router/lifecycle input is not defined until CU1. That's okay because CU0-L will capture payloads. But after CU0-L, we need to design the adapter's mapping. The plan doesn't specify the contract between adapter and engine. Should it be the same as Codex adapter? The engine expects certain fields. The plan should specify that the adapter must map the Cursor event to whatever session_router.handle_event expects. The plan says "the adapter normalizes host lifecycle events into the existing advisory engine" and "the adapter maps only that identity into the existing write-once lifecycle binding." But it doesn't define the engine's expected input schema. That's in the existing engine code. That's acceptable, but we might need to check if the existing engine's handle_event expects a specific dict shape with session_id, cwd, transcript_path, etc. The Codex adapter used that shape; Cursor adapter will need to produce the same shape. The plan should at least state that the adapter must produce a payload matching the engine's expected fields. The plan says "delegate a persisted SessionStart directly to session_router.handle_event" - so we assume that the Cursor event can be mapped to those same fields. If the Cursor event provides conversation_id, workspace_roots (array), transcript_path? The engine might expect a single cwd, not an array. The adapter must resolve the workspace root to a single cwd for the registered repo. That's a mapping detail that could be an integration risk if not specified. The plan doesn't discuss how workspace_roots is converted to cwd. That could be ambiguous. The existing session_router might expect a "cwd" field. The Codex adapter gets "cwd" directly from Codex. Cursor gives workspace_roots, which can be multiple. The engine is designed for a single repository root. Which root to use? The plan says "workspace roots" and "registered repository" - the adapter should match the registered repo root among them? But workspace_roots might just be the opened folder in Cursor, which could be multiple. The plan says "adapter must map only that identity into the existing write-once lifecycle binding." It might need to pick one root that is a registered repo. But there could be multiple. If so, which one is the session's primary? The plan says "unregistered workspace: minimal advisory behavior only." If multiple roots, some registered, some not? This is underspecified. The adapter might need to handle that. This is a potential integration gap.

- Also, Cursor sessionStart may fire with multiple workspace roots. The adapter might need to determine the primary root for the session binding. The plan doesn't specify how. This could be a source of ambiguity.

- Another point: Cursor `preCompact` event: the plan says "delegate to the existing checkpoint/start-context recovery seam without changing identity." What is that seam? The engine likely doesn't have a preCompact handler. The plan might need to define that. The engine currently handles SessionStart and SessionEnd. Does it have a checkpoint concept? In the lifecycle engine, maybe compact is just a fresh start? The plan says for Codex, "compact matcher reinjects compact context without replacing the write-once binding." In Codex, compact is just a SessionStart matcher. For Cursor, preCompact is a separate event. The adapter could map preCompact to a SessionStart with matcher=compact? But the engine doesn't have a separate compaction handler; it uses the start event. So the adapter would need to call session_router.handle_event with session_id and indicate it's a compact, maybe via a "reason" or "matcher" field. The engine's session_router might accept a "source" field. The Codex adapter used "source" and "reason". For Cursor, we could map preCompact to a SessionStart with reason="compact". That's plausible. But the plan doesn't specify that mapping. Might be fine to defer to CU1, but it's a potential gap that could be clarified.

- The architecture diagram shows Cursor session event going to cursor_session_adapter.py, which feeds session_router.py / session_lifecycle.py. That's fine. But the adapter must also handle the nonce requirement: the context injection output must include a nonce. The router's output (context injection) is the text returned to the host on SessionStart. The engine likely constructs a context message. If we need a nonce, the engine might need to be extended. The plan does not address this. This is a missing component: the nonce generation and injection mechanism. Without it, the live acceptance cannot verify context delivery. This could be considered an architectural gap.

- Another integration risk: Cursor IDE and CLI might fire events at different times or scopes. The plan says "CU4: run IDE and CLI acceptance separately" and "promotion rule: IDE and CLI pass independently. CU1 may target only a surface with a proven matching start/end pair; the other remains explicitly unsupported/degraded." That's fine, but the adapter must handle both surfaces. The plan doesn't specify whether the same adapter handles both or if they'll be separate. It says "the adapter owns only Cursor payload validation...". Could be one adapter that handles both event sources, since the payload shapes are similar. That's okay.

- Race conditions: The adapter is a subprocess spawned per event. No threading. However, if the adapter writes to the shared lifecycle files, could there be concurrent writes from two different hosts (e.g., Claude Code and Cursor at the same time)? The existing engine might use per-session files and atomic writes, but if two sessions are active simultaneously on different hosts, they would have different session IDs, so no conflict. For the shared registry, that's only written by installer, not during sessions. So no race.

- Error handling: The plan describes fail-open behavior, which is correct for advisory. For Cursor, if the adapter crashes or times out, Cursor likely continues. The adapter must be designed to not block Cursor. The plan says "fail-open / fire-and-forget". Good.

- Cost/performance: The adapter is a local Python script, startup might add latency. For Codex, measured p95 565ms, okay. For Cursor, the timeout might be different. The plan doesn't set explicit timeouts for Cursor hooks yet. Should probably define them later.

- Underspecified: The exact shape of the Cursor hook configuration is not defined. The template cursor_hooks.json.template will be created after CU0-L. The plan mentions "user-level activation preserves unknown hooks, validates the merged config, etc." That's fine. But the plan doesn't specify the hook structure (like event names, matchers). Cursor's hook schema might require matchers? The plan might need to decide if we use a wildcard or specific events. That can be later.

- The plan says for Cursor, "sessionStart fires when a new composer conversation is created and is fail-open/fire-and-forget for blocking behavior while allowing env and additional_context output." This means Cursor's hook can return additionalContext that gets injected. That's similar to Codex. So the adapter can output the context injection. Good.

- However, the plan mentions "nonce-proven context" - the nonce is part of the additional_context? The model must see it. The adapter would include a nonce in the additional_context, and then the live probe checks model response. That's doable. But where does the nonce come from? It could be generated by the adapter on SessionStart (random UUID). Then stored in the session binding. On live acceptance, the probe looks for that UUID in the model's output. That would require the adapter to generate and persist the nonce as part of the session binding (or plan). That is an addition to the existing engine, or the adapter could write it to a separate file. But the DoD says "prove the model received the exact nonce". The adapter could simply embed a nonce in the context text and also store it in the binding state so the acceptance test can retrieve it. That would require the adapter to have write access to the session state (beyond the engine). The plan says the adapter delegates to existing engine and does not change schemas. Adding a nonce field to the session binding would be a schema change, which violates "no new persistence schema". If the nonce is stored in the existing plan or binding, that's a change. Alternatively, the acceptance test could just search the model's output for a known nonce pattern; the nonce doesn't need to be persisted if the test can generate a known nonce and inject it, but the adapter would need to know that nonce. The probe could be set up to know the nonce. For example, the test could pre-define a nonce, put it in the context via the hook's additional_context, and then check the model's response. But the hook is the adapter running on start; the test can't easily control that unless the adapter reads an environment variable or something. That gets complicated. The plan isn't clear on this mechanism. This is a significant gap: the nonce delivery proof mechanism is underspecified. It could be implemented as a separate verification step that doesn't require schema change. For example, the acceptance test could run a session, capture the output, and search for a unique phrase that was injected. If the adapter always injects a unique session ID, the test can check that the model saw it. But that doesn't prove it was the exact nonce, just that the model saw the entire context. That might be enough. The DoD says "exact nonce". Might be over-specified. The plan could relax it to "prove the model received the injected context". That's doable by checking the model's response echoes something unique from the context (like the session plan's summary). That would be fine. But the plan as written might require a nonce, which could force a schema change. That's a potential P1 issue: the nonce requirement might conflict with the frozen boundary of no new persistence schema, and the mechanism is underspecified.

- Another potential gap: The plan says for Cursor, "transcript_path: null is a deliberate lower-fidelity/no-op path. A non-null path is parsed only after its real format and stability are captured in a sanitized fixture; there is no SQLite scraping, chat-directory scanning, or recency lookup." That's good. But what if Cursor's transcript_path is not a JSONL file? The fixture will show that. The plan is to add transcript projection only if stable. Good.

- Integration risk with the existing reaper: Cursor sessions that crash (no sessionEnd) will be reaper-owned. That's fine.

- The plan mentions "emergency-off command that removes only owned entries." That's for the installer. Good.

- The plan for Cursor CU0-L says the probe must install a temporary user-level hook block, validate merged JSON, run IDE and CLI, and restore pre-probe state. That's a delicate operation that could interfere with other Cursor hooks. The plan specifies capturing exact bytes and absence state, and restoring. That's well-defined. However, there's a risk: the probe's temporary hook might interfere with an existing user hook if the user has one. The plan says "install only a probe-owned temporary user-level hook block" - meaning it will add its own hook entry, preserving unknown ones. That's similar to the installer. So that should be safe.

- The probe's temporary hook might need to be compatible with Cursor's hook schema, which the documentation describes. The plan says we'll validate the merged JSON. That's okay.

- Potential issue: The probe's hook might need to be activated at user-level, which means it will apply to all Cursor sessions during the test. The plan says to close all Cursor IDE and CLI processes before installation, run experiments, and restore after each surface. That's careful. But if the operator has other unrelated Cursor sessions open elsewhere, could be impacted. However, that's acceptable for a local probe.

Now, let's grade findings.

P1 (blocker) findings need to be fixed before implementation begins. P2 important, should fix. P3 nice-to-have.

Top findings:

1. **Missing nonce injection mechanism and conflicting requirements (P1).** The DoD for Cursor slice requires proving that the model received the exact nonce, but the plan also forbids new persistence schema. How the nonce is generated, included, and verified without